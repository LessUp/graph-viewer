name: Deploy to GitHub Pages

on:
  push:
    branches: [master, main]
    paths:
      - 'app/**'
      - 'components/**'
      - 'hooks/**'
      - 'lib/**'
      - 'public/**'
      - 'package.json'
      - 'package-lock.json'
      - 'next.config.*'
      - 'scripts/build-static-export.mjs'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages-${{ github.ref }}'
  cancel-in-progress: true

env:
  NODE_VERSION: '22'
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    env:
      GITHUB_PAGES: 'true'
      GITHUB_REPOSITORY: ${{ github.repository }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          static_site_generator: next
        id: pages

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Restore node_modules cache
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-modules-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        run: npm ci

      - name: Build static export
        run: npm run build:static

      - name: Generate sitemap
        run: node scripts/generate-sitemap.mjs
        env:
          SITE_URL: ${{ steps.pages.outputs.base_url }}
          OUTPUT_DIR: ./out

      - name: Setup SPA routing
        run: |
          # 确保 404.html 存在
          if [ ! -f "./out/404.html" ]; then
            cp ./out/index.html ./out/404.html
          fi

          # 确保 editor 目录存在
          if [ ! -d "./out/editor" ]; then
            mkdir -p ./out/editor
            REPO_NAME=$(echo ${{ github.repository }} | cut -d'/' -f2)
            printf '<!DOCTYPE html><html><head><meta charset="UTF-8"><meta http-equiv="refresh" content="0;url=/${REPO_NAME}/"><script>window.location.href="/${REPO_NAME}/";</script></head><body>Redirecting...</body></html>' > ./out/editor/index.html
          fi

      - name: Build summary
        run: |
          TOTAL_SIZE=$(du -sh ./out/ | cut -f1)
          FILE_COUNT=$(find ./out/ -type f | wc -l)
          echo "## 📦 Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Total Size | **$TOTAL_SIZE** |" >> $GITHUB_STEP_SUMMARY
          echo "| Total Files | $FILE_COUNT |" >> $GITHUB_STEP_SUMMARY

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
        id: deployment

      - name: Deployment summary
        run: |
          echo "## 🚀 Deployment Complete" >> $GITHUB_STEP_SUMMARY
          echo "URL: ${{ steps.deployment.outputs.page_url }}" >> $GITHUB_STEP_SUMMARY
